import{useState}from'react';import{useNavigate}from'react-router-dom';import PollForm from'../components/PollForm';import{createPoll}from'../services/pollService';import{errorMessage}from'../services/api';export default function CreatePoll(){const[error,setError]=useState('');const nav=useNavigate();const submit=async data=>{try{const p=await createPoll(data);nav(`/polls/${p.id}`)}catch(e){setError(errorMessage(e))}};return <><h1>Kreiraj anketu</h1>{error&&<p className="error">{error}</p>}<PollForm onSubmit={submit} buttonText="Kreiraj anketu"/></>}
