import{Link}from'react-router-dom';export default function PollCard({poll}){return <article className="card"><span className="tag">{poll.categoryName}</span><h3><Link to={`/polls/${poll.id}`}>{poll.title}</Link></h3><p>{poll.description||'Bez opisa'}</p><small>{poll.active?'Aktivna':'Zatvorena'} · {poll.totalVotes} glasova · {new Date(poll.createdAt).toLocaleDateString('sr-RS')}</small></article>}
